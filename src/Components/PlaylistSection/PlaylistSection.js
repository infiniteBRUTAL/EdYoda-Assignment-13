import React from 'react'import axios from 'axios'import classes from './PlaylistSection.module.css'import VideoCard from "../VideoCard/VideoCard";import { PLAYLIST_API} from "../../Utils/APIEndpoints";class PlaylistSection extends React.Component {    state = {        response : [],    }    componentDidMount() {        axios.get(PLAYLIST_API)            .then((response) => {                this.setState({                    response : response.data                })            })        this.props.history.push('/1')    }    render() {        return(            <div className={classes.MainContainer}>                {this.state.response.map(data => <VideoCard ID={this.props.videoId} parentState={this.props.parentState} {...this.props} key={data.id} vidId={data.id} thumbnail={data.thumbnail} title={data.title}/>)}            </div>        )    }}export default PlaylistSection;