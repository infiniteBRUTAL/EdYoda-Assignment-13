import React from 'react'import axios from 'axios'import classes from './PlaylistSection.module.css'import VideoCard from "../VideoCard/VideoCard";class PlaylistSection extends React.Component {    state = {        response : [],    }    componentDidMount() {        axios.get('https://5d76bf96515d1a0014085cf9.mockapi.io/playlist')            .then((response) => {                this.setState({                    response : response.data                })            })        console.log(this.state.response)    }    render() {        console.log(this.props)        return(            <div className={classes.MainContainer}>                {this.state.response.map(data => <VideoCard ID={this.props.match.params.vid} parentState={this.props.parentState} history={this.props.history} key={data.id} vidId={data.id} thumbnail={data.thumbnail} title={data.title}/>)}            </div>        )    }}export default PlaylistSection;